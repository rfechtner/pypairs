"""
    #category_names = np.unique(adata.obs['category'])
    #categories = defaultdict(list)

    #for name in category_names:
    #    mask = np.isin(adata.obs['category'], name)
    #    sample_names = adata.obs['name']
    #    categories[name] = list(sample_names[mask])

    #pairs.sandbag(adata, annotation=categories)
"""
